The python program is Caesar cipher algorithm that encrypts and decrypts data. It allows the user to enter or provide the shift mode or number and it will process the text and give the desired output.
